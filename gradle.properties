org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# Only read when building THIS repo (Gradle ignores a subproject's
# gradle.properties, and host apps never have this key). Pins react-android for
# the :passpoint-react-native verification module to the oldest React Native the
# SDK supports, so the bridge is checked against its compatibility floor.
heliumReactAndroidVersion=0.73.11
